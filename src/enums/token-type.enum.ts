enum EToken {
  ACCESS = "access",
  REFRESH = "refresh",
  FORGOT = "forgot",
  ACTIVE = "active",
}
enum EActionTokenType {
  FORGOT = "forgot",
  ACTIVE = "active",
}

export { EToken, EActionTokenType };
